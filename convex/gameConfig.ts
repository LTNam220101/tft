/**
 * Set the app uses for list/optimize UI (champions, traits, items).
 * After seeding Set 17, change to `"TFTSet17"` to switch without deleting Set 16 rows.
 */
export const ACTIVE_SET_KEY = "TFTSet17" as const
